// src/hooks/useAuth.js
// React Hook to access AuthContext.
// Eliminates repetitive imports of useContext and AuthContext.

import { useAuth } from '../context/AuthContext';
export default useAuth;
